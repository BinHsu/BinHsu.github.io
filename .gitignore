# Jekyll build artefacts
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Local dev (we don't carry Gemfile.lock to avoid platform drift)
Gemfile.lock

# OS / editor noise
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Agent onboarding doc — local-only, contains operational paths + OPSEC notes
# that don't need to be on the public Pages site. Lives on Bin's machine(s);
# new machines need it recreated or synced via another channel.
CLAUDE.md
